{
  "predict": {
    "traces": [],
    "train": [],
    "demos": [
      {
        "augmented": true,
        "assembly": "int FUN(int a){int b=a*2;return b;}",
        "code": "int func(int a) { return a * 2; }"
      },
      {
        "augmented": true,
        "assembly": "int FUN(int x){int y=x+1;return y;}",
        "code": "int func(int x) { return x + 1; }"
      }
    ],
    "signature": {
      "instructions": "Given the fields `assembly`, produce the fields `code`.",
      "fields": [
        {
          "prefix": "Assembly:",
          "description": "${assembly}"
        },
        {
          "prefix": "Code:",
          "description": "${code}"
        }
      ]
    },
    "lm": null
  },
  "metadata": {
    "dependency_versions": {
      "python": "3.10",
      "dspy": "3.1.3",
      "cloudpickle": "3.1"
    }
  }
}
